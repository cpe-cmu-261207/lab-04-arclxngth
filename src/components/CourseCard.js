export const CourseCard = (props) => {
  // TODO: design HTML component that displays a course as a "card" on the webpage.
  
  return (
  <div>
    Course's ID : {props.course_id} Grade : {props.grade} Credit : {props.credit}
  </div>
  );
};
